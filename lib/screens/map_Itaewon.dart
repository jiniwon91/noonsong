import 'package:flutter/material.dart';

class Map_Itaewon extends StatelessWidget {
  const Map_Itaewon({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Map Screen'),
      ),
      body: const Center(
        child: Text('This is Itaewon'),
      ),
    );
  }
}
