import 'package:flutter/material.dart';

class Map_Haebangchon extends StatelessWidget {
  const Map_Haebangchon({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Map Screen'),
      ),
      body: const Center(
        child: Text('This is 해방촌'),
      ),
    );
  }
}
